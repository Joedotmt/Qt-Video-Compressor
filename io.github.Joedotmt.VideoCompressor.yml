app-id: io.github.Joedotmt.VideoCompressor
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: video-compressor
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
modules:
  - name: video-compressor
    buildsystem: simple
    build-commands:
      - install -Dm755 packaging/linux/video-compressor /app/bin/video-compressor
      - install -Dm644 main.py /app/share/video-compressor/main.py
      - install -Dm644 compressor.py /app/share/video-compressor/compressor.py
      - install -Dm644 data/io.github.Joedotmt.VideoCompressor.desktop /app/share/applications/io.github.Joedotmt.VideoCompressor.desktop
      - install -Dm644 data/io.github.Joedotmt.VideoCompressor.metainfo.xml /app/share/metainfo/io.github.Joedotmt.VideoCompressor.metainfo.xml
      - install -Dm644 data/icons/io.github.Joedotmt.VideoCompressor.svg /app/share/icons/hicolor/scalable/apps/io.github.Joedotmt.VideoCompressor.svg
      - install -Dm644 data/icons/io.github.Joedotmt.VideoCompressor-symbolic.svg /app/share/icons/hicolor/symbolic/apps/io.github.Joedotmt.VideoCompressor-symbolic.svg
    sources:
      - type: dir
        path: .
        skip:
          - .git
          - .venv
          - .flatpak-builder
          - build-dir
          - __pycache__
          - '*.pyc'
